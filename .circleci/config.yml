version: 2.1

orbs:
  welcome: circleci/welcome-orb@0.3.1

jobs:
  build:
    steps:
      - run:
	name: Tests
	shell: /bin/sh
        command: |
	  echo Running test
          python3 -m unittest discover -s tests -p *_test.py
