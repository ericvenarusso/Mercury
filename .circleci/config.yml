version: 2.0
jobs:
 build:
   docker:
     - image: circleci/node:10-browsers
   steps:
     - run:
         name: Tests
         command: |
           echo 'Running tests'
           TESTFILES=$(circleci tests glob 'tests/**.py')
           echo TESTFILES
           python -m unittest ${TESTFILES}

